<div class="container mt-4">
    {{#if semProdutos}}
        <div class="card text-center shadow border-0 mt-5">
            <div class="card-body p-5">
                <h1 class="display-4 text-muted mb-4"><i class="bi bi-shop-window"></i></h1>
                <h2 class="card-title text-secondary">Ops! Nossa vitrine está vazia.</h2>
                <p class="card-text lead">Parece que todos os produtos esgotaram ou ainda não foram cadastrados.</p>
                <hr class="my-4">
                <p>Para visualizar os produtos de teste, por favor realize o povoamento do sistema.</p>
                <a href="/povoamento" class="btn btn-primary btn-lg">
                    <i class="bi bi-database-add"></i> Realizar Povoamento
                </a>
            </div>
        </div>
    {{else}}

    <div class="row row-cols-1 row-cols-md-2 row-cols-lg-4 g-4">
        {{#each produtos}}
        <div class="col">
            <div class="card h-100 shadow-sm">
                <div class="card-body">
                    <h5 class="card-title text-truncate" title="{{this.nome}}">{{this.nome}}</h5>
                    <p class="card-text mt-3">
                        <span class="d-block">Preço: <strong class="text-success">R$ {{this.preco}}</strong></span>
                        <span class="d-block text-muted small">Disponíveis: {{this.estoque}}</span>
                    </p>
                </div>
                <div class="card-footer bg-white border-top-0 d-flex justify-content-between align-items-center mb-2">
                    <a href="/produto/{{this.id}}" class="btn btn-outline-secondary btn-sm">
                        <i class="bi bi-box-arrow-up-right"></i> Detalhes
                    </a>
                    <a href="/comprar/{{this.id}}" class="btn btn-success btn-sm">
                        <i class="bi bi-bag-check-fill"></i> Comprar
                    </a>
                </div>
            </div>
        </div>
        {{/each}}
    </div>
    {{/if}}
</div>
